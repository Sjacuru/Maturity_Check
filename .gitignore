# Claude Code local settings and memory (machine-specific)
.claude/

# Environment and secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
pip-log.txt
pip-delete-this-directory.txt
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.venv/
venv/
ENV/
env/

# Node (if you add a JS/TS frontend or tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE / editor
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Local LLM / ML artifacts (common paths — adjust as needed)
*.gguf
*.bin
models/
checkpoints/

# Jupyter
.ipynb_checkpoints/

# Logs and temp
*.log
tmp/
temp/
.cache/

# LanceDB vector store (derived from SQLite path; never committed)
lancedb/

# Project data artifacts — ignore everything in data/ by default,
# but track source-of-truth JSON artifacts in known subdirectories.
# New directories (e.g. case_documents/, pdfs/) remain ignored automatically.
data/*
!data/ipmp/
!data/rio_manual/
!data/acronyms/
!data/retrieval_profiles/
# Machine-specific SSL cert bundle (corporate proxy CA)
data/corporate_ca.pem
# Ignore PDFs anywhere under data/ in case they land in a tracked subdirectory
data/**/*.pdf

# Real PPP process documents (Phase B working material) — never committed
docs/SMG-040_101607_2024_*.pdf

# Personal teaching workspace (not part of the deliverable)
Learning/
